name: gsc-hnn-siads
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - python>=3.10
  - numpy>=1.24
  - scipy>=1.10
  - matplotlib>=3.7
  - pytest>=7.0
  - pip
  - pip:
      - torch>=2.0
